// Thought API Routes for api/thoughts/

const router = require("express").Router();

const {
  getAllThoughts,
  getOneThought,
  createThought,
  deleteThought,
} = require("../../controllers");
// TODO: Set up controller paths


